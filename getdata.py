import bs4
import os
import requests

N=18
ddir="data"
url=""

if(os.path.exists(ddir) and os.path.isdir(ddir)):
    ddir=""
else:
    requests.

for n in range(1,N):
    with open(ddir/str(n)) as f:
        page=f.read()

        =page